---
###########################
###########################
## Markdown Linter rules ##
###########################
###########################

# Linter rules doc:
# - https://github.com/DavidAnson/markdownlint
#
# Note:
# To comment out a single error:
#   <!-- markdownlint-disable -->
#   any violations you want
#   <!-- markdownlint-restore -->
#

###############
# Rules by id #
###############
MD004: false # Unordered list style
MD007:
  indent: 2 # Unordered list indentation
MD013:
  line_length: 400 # Line length 80 is far too short
  # Tables and fenced code blocks are exempt because compliance is not
  # achievable there: MARKDOWN_PRETTIER pads table cells to align pipes, so a
  # table with real content can exceed any limit, and the ffmpeg parameter
  # strings in apps/dispatcharr/README.md must stay on one line to be
  # copy-pasteable into Dispatcharr's UI.
  code_blocks: false
  tables: false
MD026:
  punctuation: ".,;:!。，；:" # List of not allowed
MD029: false # Ordered list item prefix
MD033: false # Allow inline HTML
MD036: false # Emphasis used instead of a heading

#################
# Rules by tags #
#################
blank_lines: false # Error on blank lines
