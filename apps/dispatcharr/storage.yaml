---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: dispatcharr-config-pv
spec:
  capacity:
    storage: 15Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-storage
  local:
    path: /mnt/internal/dispatcharr/config
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - homeserver
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dispatcharr-config-pvc
  namespace: dispatcharr
spec:
  storageClassName: local-storage
  volumeName: dispatcharr-config-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 15Gi