apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minio-test
  namespace: minio
  annotations:
    kubernetes.io/ingress.class: "nginx"
    kubernetes.io/tls-acme: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    cert-manager.io/cluster-issuer: "letsencrypt"
    nginx.ingress.kubernetes.io/configuration-snippet: |
      if ($request_uri ~ ^/(.*/)?$) {
        rewrite ^/(.*/)?$ /test/$1index.html break;
      }
      if ($request_uri ~ ^/(.*/)?(.*)$) {
        rewrite ^/(.*/)?(.*)$ /test/$1$2 break;
      }
spec:
  tls:
    - hosts:
        - test.tmem.de
      secretName: test-tls
  rules:
    - host: test.tmem.de
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: minio
                port:
                  number: 9000