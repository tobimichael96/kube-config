---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2beta2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app tvheadend
  namespace: *app
spec:
  chart:
    spec:
      chart: app-template
      version: 3.7.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  interval: 5m
  releaseName: *app
  targetNamespace: *app
  values:
    controllers:
      tvheadend:
        containers:
          app:
            image:
              repository: linuxserver/tvheadend
              tag: latest
            env:
              TZ: Europe/Berlin
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /
                    port: 9981
                  initialDelaySeconds: 5
                  periodSeconds: 10
                  timeoutSeconds: 3
                  failureThreshold: 5
              readiness: *probes
              startup:
                enabled: false
          # Configure the gluetun sidecar
          gluetun:
            dependsOn: app
            image:
              repository: ghcr.io/qdm12/gluetun
              tag: v3.40.0
            env:
              TZ: Europe/Berlin
              FIREWALL: on
              FIREWALL_INPUT_PORTS: 9982,9981,9999
              VPN_TYPE: wireguard
              VPN_SERVICE_PROVIDER: mullvad
              SERVER_COUNTRIES: Germany
              UPDATER_PERIOD: 12h
              DNS_KEEP_NAMESERVER: on
              OPENVPN_PASSWORD: m
              HEALTH_SERVER_ADDRESS: "0.0.0.0:9999"
            envFrom:
              - secret: tvheadend-vpn-config
            # Configure the container-specific securityContext
            securityContext:
              capabilities:
                add:
                  - NET_ADMIN
                  - SYS_MODULE
            probes:
              liveness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /
                    port: 9999
                  initialDelaySeconds: 30
                  periodSeconds: 10
                  timeoutSeconds: 3
                  failureThreshold: 12
              startup:
                enabled: false
    ingress:
      app:
        annotations:
          nginx.ingress.kubernetes.io/proxy-body-size: 5G
          kubernetes.io/ingress.class: "nginx-internal"
          kubernetes.io/tls-acme: "true"
          nginx.ingress.kubernetes.io/ssl-redirect: "true"
          cert-manager.io/cluster-issuer: "letsencrypt"
        hosts:
          - host: &host tvheadend.internal.tmem.de
            paths:
              - path: /
                service:
                  identifier: app
                  port: http
        tls:
          - hosts:
              - *host
            secretName: tvheadend-tls
    persistence:
      config:
        enabled: true
        advancedMounts:
          tvheadend:
            app:
              - path: /config
        existingClaim: "tvheadend-config-pvc"
      recordings:
        enabled: true
        type: emptyDir
        advancedMounts:
          tvheadend:
            app:
              - path: /recordings
      logs:
        type: emptyDir
        advancedMounts:
          tvheadend:
            app:
              - path: /config/logs
      vpn-config:
        enabled: true
        type: secret
        name: tvheadend-vpn-config
        advancedMounts:
          tvheadend:
            gluetun:
              - path: /gluetun/wireguard/
    service:
      app:
        controller: *app
        ports:
          http:
            port: 9981
      app-additional:
        controller: *app
        ports:
          tcp:
            port: 9982
      gluetun:
        controller: *app
        ports:
          http:
            port: 9999
