apiVersion: v1
kind: PersistentVolume
metadata:
  name: homebox-pv
spec:
  capacity:
    storage: 10Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-storage
  local:
    path: /mnt/internal/homebox
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - tmcloud
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homebox-pvc
  namespace: homebox
spec:
  storageClassName: local-storage
  volumeName: homebox-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi