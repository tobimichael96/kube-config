---
apiVersion: helm.cattle.io/v1
kind: HelmChartConfig
metadata:
  name: rke2-coredns
  namespace: kube-system
spec:
  # language=yaml
  valuesContent: |-
    tolerations:
      - effect: NoSchedule
        key: cloud
        operator: Exists
